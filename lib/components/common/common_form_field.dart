import 'package:flutter/material.dart';

class CommonFormFiled extends StatelessWidget {
  const CommonFormFiled({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
